define(function(require){
  var
    utils = require('./utils'){{#if variables}}
  {{#each variables}}
  , {{{this.name}}} = {{{this.value}}}
  {{/each}}{{/if}}
  ;{{#if body}}
  {{{body}}}{{/if}}

  return {{{$return}}};
});