function({{#each idVars}}{{this}}, {{/each}}query, callback){
  if (typeof query === 'function'){
    callback = query;
    query = {};
  }

  utils.get({{#each urlParts}}{{#if this.isUrl}}'/{{this.part}}/'{{else}}{{this.part}} {{/if}} + {{/each}}utils.query(query), callback);
}
